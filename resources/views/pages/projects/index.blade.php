@extends('layouts.app', ['title' => 'Projects | Queue Company'])
@section('content')
<div class="container"><h1 class="mb-4">Projects</h1><div class="row g-4">@forelse($projects as $project)<div class="col-md-6 col-lg-4"><div class="card h-100"><img src="{{ $project->featured_image ?: 'https://placehold.co/600x350' }}" class="card-img-top" alt="{{ $project->title }}"><div class="card-body d-flex flex-column"><div class="mb-2"><span class="badge bg-primary-subtle text-primary">{{ $project->category }}</span> <span class="badge bg-secondary">{{ ucfirst(str_replace('_',' ',$project->status)) }}</span></div><h5>{{ $project->title }}</h5><p class="text-muted">{{ $project->short_description }}</p><div class="mt-auto d-flex gap-2"><a href="{{ route('projects.show',$project->slug) }}" class="btn btn-sm btn-primary">View Details</a><a href="{{ $project->demo_url ?: route('demo-login') }}" class="btn btn-sm btn-outline-dark" @if($project->demo_url)target="_blank"@endif>Demo</a></div></div></div></div>@empty<div class="col-12"><div class="alert alert-info">No projects available right now.</div></div>@endforelse</div><div class="mt-4">{{ $projects->links() }}</div></div>
@endsection
