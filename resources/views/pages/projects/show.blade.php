@php($settings = \App\Models\SiteSetting::current())
@extends('layouts.app', ['title' => $project->meta_title ?: $project->title, 'metaDescription' => $project->meta_description ?: $project->short_description])
@section('content')
<section class="py-5 bg-soft border-bottom"><div class="container"><nav class="small mb-3"><a href="{{ route('projects.index') }}">{{ __('messages.nav.projects') }}</a> / {{ $project->title }}</nav><h1 class="mb-2">{{ $project->title }}</h1><p class="lead mb-3">{{ $project->short_description }}</p><div class="d-flex gap-2 flex-wrap"><span class="badge text-bg-light border">{{ $project->category }}</span><span class="badge status-badge status-{{ $project->status }}">{{ __('messages.labels.status_'.$project->status) }}</span>@if($project->industry)<span class="badge text-bg-light border">{{ $project->industry }}</span>@endif</div></div></section>
<section class="py-6"><div class="container"><div class="row g-4"><div class="col-lg-8"><img src="{{ $project->featured_image_url }}" class="img-fluid rounded-4 mb-4" alt="{{ $project->title }}"><div class="surface-card mb-4"><h3>Overview</h3><p class="mb-0">{{ $project->full_description }}</p></div></div>
<div class="col-lg-4"><aside class="credentials-box p-4 sticky-lg-top"><h4 class="mb-3">{{ __('messages.demo_credentials') }}</h4>@if($project->demo_url)<button type="button" class="btn btn-primary w-100 rounded-pill demo-modal-trigger" data-bs-toggle="modal" data-bs-target="#demoModal" data-demo-url="{{ $project->demo_url }}" data-demo-username="{{ $project->demo_username }}" data-demo-password="{{ $project->demo_password }}">{{ __('messages.buttons.open_demo') }}</button>@else<div class="d-grid gap-2"><a class="btn btn-outline-success rounded-pill" target="_blank" href="https://wa.me/{{ preg_replace('/\D+/', '', $settings->whatsapp_number ?? '') }}">{{ __('messages.buttons.whatsapp') }}</a><a class="btn btn-outline-primary rounded-pill" target="_blank" href="{{ $settings->telegram_link ?: '#' }}">{{ __('messages.buttons.telegram') }}</a></div>@endif</aside></div></div></div></section>
@include('partials.demo-modal')
@endsection
